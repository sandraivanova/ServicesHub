{
  "name": "dnevnica",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=packages/models"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
